# Zabbix Agent: сбор метрик, PostgreSQL, мониторинг и Ansible

Проект команды 22 по автоматизации установки и настройки Zabbix Agent с сохранением метрик в PostgreSQL.

## 📋 Содержание

- [Структура проекта](#структура-проекта)
- [Распределение метрик](#распределение-метрик)
- [Быстрый старт](#быстрый-старт)
- [Документация](#документация)
- [Отчет](#отчет)
- [Требования](#требования)

## 📁 Структура проекта

```
.
├── ansible/                    # Ansible автоматизация
│   ├── inventories/           # Инвентари серверов
│   │   └── inventory.yml      # Файл инвентаря
│   ├── roles/                 # Ansible роли
│   │   └── zabbix_agent/      # Роль для установки Zabbix Agent
│   │       ├── tasks/         # Задачи роли
│   │       ├── templates/     # Шаблоны Jinja2
│   │       └── handlers/      # Обработчики
│   ├── playbook.yml           # Главный playbook
│   └── ansible.cfg            # Конфигурация Ansible
│
├── configs/                   # Конфигурационные файлы
│   └── zabbix_agentd.conf.example  # Пример конфига Zabbix Agent
│
├── sql/                       # SQL скрипты
│   └── create_database.sql    # Создание базы данных
│
├── scripts/                   # Bash скрипты
│   ├── test_zabbix_agent.sh   # Тестирование агента
│   ├── view_metrics.sh        # Просмотр метрик из БД
│   ├── setup_cron.sh          # Настройка cron для автосбора
│   └── export_metrics_report.sh  # Экспорт метрик в CSV
│
├── report/                    # Отчет команды
│   └── Team22_Report.docx     # Word документ с отчетом (добавить после выполнения)
│
├── README.md                  # Этот файл
├── TEAM_GUIDE.md              # Пошаговое руководство для команды
├── MANUAL_INSTALLATION.md     # Инструкция по ручной установке
├── UNIFYING_SERVERS.md        # Объединение серверов в единый
├── QUICKSTART.md              # Быстрый старт
└── requirements.txt           # Требования к системе
```

## 🎯 Распределение метрик

| Студент   | Username | Метрика             | Описание                                    |
| --------- | -------- | ------------------- | ------------------------------------------- |
| Студент 1 | user1    | CPU Load            | Загрузка процессора (1 минута)              |
| Студент 2 | user2    | RAM Usage           | Использование оперативной памяти (%)        |
| Студент 3 | user3    | Disk Availability   | Доступность диска (%)                       |
| Студент 4 | user4    | Network Connections | Количество установленных сетевых соединений |
| Студент 5 | user5    | Processes           | Количество запущенных процессов             |
| Студент 6 | user6    | Swap Usage          | Использование swap памяти (%)               |

## 🚀 Быстрый старт

### Порядок выполнения

**Важно**: Сначала выполните ручную установку, затем автоматизируйте через Ansible.

1. **Ручная установка** → См. [MANUAL_INSTALLATION.md](MANUAL_INSTALLATION.md)
2. **Автоматизация через Ansible** → См. [QUICKSTART.md](QUICKSTART.md)
3. **Объединение серверов** → См. [UNIFYING_SERVERS.md](UNIFYING_SERVERS.md)

### Минимальная установка через Ansible

```bash
# 1. Настройте переменные в ansible/playbook.yml
# 2. Настройте inventory в ansible/inventories/inventory.yml
# 3. Запустите playbook
cd ansible
ansible-playbook playbook.yml
```

## 📚 Документация

### Основные документы

- **[MANUAL_INSTALLATION.md](MANUAL_INSTALLATION.md)** - Подробная инструкция по ручной установке всех компонентов

  - Установка Midnight Commander
  - Настройка локали en_US.UTF-8
  - Установка Zabbix Agent
  - Настройка метрик
  - Настройка PostgreSQL
  - Настройка автоматического сбора

- **[UNIFYING_SERVERS.md](UNIFYING_SERVERS.md)** - Описание процесса объединения серверов в единую систему

  - Архитектура объединенного решения
  - Процесс настройки централизованного Zabbix Server
  - Настройка единой базы данных PostgreSQL
  - Регистрация всех хостов в Zabbix
  - Создание единого дашборда
  - **Примечание**: Согласно заданию, нужно описать процесс в отчете, практическое выполнение опционально

- **[QUICKSTART.md](QUICKSTART.md)** - Быстрый старт с Ansible

  - Настройка переменных
  - Запуск playbook
  - Проверка работы

- **[TEAM_GUIDE.md](TEAM_GUIDE.md)** - Пошаговое руководство для команды ⭐
  - Инструкции для каждого участника
  - Что скриншотить на каждом этапе
  - Требования к отчету
  - Чеклист выполнения

### Дополнительные материалы

- **[requirements.txt](requirements.txt)** - Требования к системе и установка зависимостей
- **[configs/README.md](configs/README.md)** - Описание конфигурационных файлов
- **[report/README.md](report/README.md)** - Требования к отчету

## 📊 Отчет

Отчет команды должен находиться в файле `report/Team22_Report.docx`.

**Структура отчета:**

1. Введение (описание задачи, состав команды)
2. Ручная установка (шаги с скриншотами)
3. Автоматизация через Ansible
4. Объединение серверов в единый
5. Результаты (метрики, данные в БД, выводы)

**Ссылка на отчет:** [report/Team22_Report.docx](report/Team22_Report.docx) _(добавить после выполнения)_

## ⚙️ Требования

### Системные требования

- **ОС**: Debian minimal (английский язык, locale en_US.UTF-8)
- **Python**: 3.6+
- **Ansible**: 2.9+
- **PostgreSQL**: 12+
- **Zabbix Server**: для централизованного мониторинга
- **SSH доступ**: к целевым серверам

### Установка зависимостей

См. подробные инструкции в [requirements.txt](requirements.txt).

```bash
# Ansible
sudo apt-get update
sudo apt-get install ansible

# PostgreSQL
sudo apt-get install postgresql postgresql-client

# Zabbix Get (для тестирования)
sudo apt-get install zabbix-get
```

## 🔧 Использование

### Проверка метрик

```bash
# Через zabbix_get
zabbix_get -s <server_ip> -k user1.cpu

# Через скрипт
./scripts/test_zabbix_agent.sh user1 cpu

# Просмотр в PostgreSQL
./scripts/view_metrics.sh user1
```

### Экспорт данных для отчета

```bash
./scripts/export_metrics_report.sh user1
```

## 🗄️ База данных PostgreSQL

### Структура таблицы

Каждая таблица имеет следующую структуру:

```sql
CREATE TABLE username_zabbix (
    id SERIAL PRIMARY KEY,
    timestamp TIMESTAMP NOT NULL,
    metric_type VARCHAR(50) NOT NULL,
    metric_value VARCHAR(100) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Примеры запросов

```sql
-- Последние 10 записей
SELECT * FROM user1_zabbix ORDER BY timestamp DESC LIMIT 10;

-- Статистика по всем метрикам команды
SELECT 'user1' as user, COUNT(*) as count FROM user1_zabbix
UNION ALL
SELECT 'user2', COUNT(*) FROM user2_zabbix
UNION ALL
SELECT 'user3', COUNT(*) FROM user3_zabbix
UNION ALL
SELECT 'user4', COUNT(*) FROM user4_zabbix
UNION ALL
SELECT 'user5', COUNT(*) FROM user5_zabbix
UNION ALL
SELECT 'user6', COUNT(*) FROM user6_zabbix;
```

## 🔍 Troubleshooting

### Агент не собирает данные

1. Проверьте статус службы:

```bash
sudo systemctl status zabbix-agent
```

2. Проверьте конфигурацию:

```bash
sudo zabbix_agentd -t user1.cpu
```

3. Проверьте логи:

```bash
tail -f /var/log/zabbix/zabbix_agentd.log
```

### Проблемы с PostgreSQL

1. Проверьте подключение:

```bash
psql -h localhost -U postgres -d zabbix_metrics -c "SELECT 1;"
```

2. Проверьте права пользователя:

```sql
GRANT ALL PRIVILEGES ON TABLE userX_zabbix TO postgres;
```

## 📝 Порядок выполнения задания

1. ✅ **Ручная установка** - выполните все шаги вручную (см. MANUAL_INSTALLATION.md)
2. ✅ **Документирование** - сделайте скриншоты процесса установки
3. ✅ **Автоматизация** - создайте/запустите Ansible роль для автоматизации
4. ✅ **Описание объединения** - опишите в отчете, как объединить все серверы в единый (см. UNIFYING_SERVERS.md)
5. ✅ **Отчет** - создайте Word документ с описанием всех шагов
6. ✅ **Репозиторий** - загрузите все в git.fiit.ssau.ru

## 👥 Авторы

Команда 22

## 📄 Лицензия

Учебный проект

---

**Репозиторий**: git.fiit.ssau.ru  
**Отчет**: [report/Team22_Report.docx](report/Team22_Report.docx)
